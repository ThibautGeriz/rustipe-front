import * as ImagePicker from 'expo-image-picker';

export const { MediaTypeOptions } = ImagePicker;
export const launchImageLibraryAsync = jest.fn();
export const requestCameraRollPermissionsAsync = jest.fn();
